install.packages(
  c(
    "twitteR",
    "httr",  # required for TwitteR OAuth
    "base64enc",  # required for TwitteR OAuth
    "tm",
    "wordcloud",
    "jsonlite"
  ),
  repos = "http://cran.us.r-project.org"
)
